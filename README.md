# pflags

## Build and install
  ```sh
  rm -f -r /tmp/pflags
  git clone https://github.com/chahal-p/pflags.git /tmp/pflags
  cd /tmp/pflags
  make build
  sudo make install
  cd -
  ```

## Usage
  ```sh
  pflags --help
  ```
