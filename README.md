# pflags

## Build and install
  #### Clone the repo
  ```
  git clone https://github.com/chahal-p/pflags.git
  ```
  #### Build binary
  ```
  make build
  ```
  #### Build binary
  ```
  sudo cp ./out/pflags /usr/local/bin/
  ```
