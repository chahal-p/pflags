# pflags

## Build and install
  #### Clone the repo
  ```
  git clone https://github.com/chahal-p/pflags.git
  ```
  #### Build binary
  ```
  make build
  ```
  #### Install
  ```
  sudo make install
  ```
